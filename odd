#include<stdio.h>

int main(){
    int number;
    printf("enter number",number);
    scanf("%d\n",&number);
    if(number%2==0){
        break;
    }
    else {
        continue;
    }
    return 0;
}